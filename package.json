{
  "name": "cectron",
  "version": "1.0.0",
  "description": "Administration system software for all demands",
  "main": "main.js",
  "scripts": {
    "start": "electron main.js",

    "script":"",
    "build":"electron-packager . --out=dist/win --platform=win32 --arch=mx64 --icon=images/logos/logo.ico --asar=true --overwrite",

    "script": "",
    "dev:electron-main": "cross-env NODE_ENV='development' electron -r babel-register ./",
    "build": "electron-packager . --out=dist/win --platform=win32 --arch=mx64 --icon=images/logos/logo.ico --asar=true --overwrite"

  },
  "repository": "https://github.com/reddeath1/cectron.git",
  "keywords": [
    "nexar"
  ],
  "author": "Frank Galos <frankslayer1@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "electron": "^2.0.2",
    "electron-watch": "^1.0.8"
  },
  "devDependencies": {
    "asar": "^0.14.3",
    "electron-packager": "^12.1.0",
    "watch": "^1.0.2"
  }
}
